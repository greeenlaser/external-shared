# Lodepng

Version: undefined (https://github.com/lvandeve/lodepng/commit/ed6fe5825c6a4fbb7f58ab35a4231c7543cd452a)

This project was built with [KalaMake](https://github.com/kalakit/kalamake). Use the `.kmake` file with the kalamake CLI to compile [lodepng](https://github.com/lvandeve/lodepng) from source.

When compiling this library from scratch:
- Copy `lodepng.cpp` to `src` and `lodepng.h` to `include`, rename `lodepng.cpp` to `lodepng.c`.