# FreeType

This project was built with [KalaMake](https://github.com/kalakit/kalamake). Copy the `ftmodule.h` file to src dir and use the `.kmake` file with the kalamake CLI to compile [FreeType](https://sourceforge.net/projects/freetype) from source.
