# FreeType

Version: 2.14.3

This project was built with [KalaMake](https://github.com/kalakit/kalamake). Copy the `ftmodule.h` file to freetype repository dir into the include directory and use the `.kmake` file with the kalamake CLI to compile [FreeType](https://sourceforge.net/projects/freetype) from source.
